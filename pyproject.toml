[project]
name = "mcp-nba-server"
version = "0.1.0"
description = "MCP server exposing the 2025-26 NBA daily leaders dataset"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.27.0",
    "asyncpg>=0.30.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
